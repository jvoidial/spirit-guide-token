name: Voxel Resonance Bot

on:
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Run bot (secure)
        env:
          PRIVATE_KEY: ${{ secrets.PRIVATE_KEY }}
        run: python3 bot.py || true
